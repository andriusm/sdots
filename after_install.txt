
USER=username
sudo useradd -m -G wheel $USER
sudo passwd $USER

sudo pacman -Syu
sudo pacman -S htop lf tree fzf go neovim gdu cmake

mkdir -p $HOME/.local/share/tig
mkdir -p $HOME/.local/state/bash

